#include<bits/stdc++.h>
using namespace std;

void solve()
{
   int n = 0, k = 0, m = 0;
   cin >> n >> m >> k;

   vector<int> A(n);
   for(int i = 0; i < n; i++)
      cin >> A[i];
   
   
}

int main ()
{
   ios::sync_with_stdio(false);
   cin.tie(nullptr);

   int t = 0; 
   cin >> t;

   for( ; t > 0; t--, solve());
   
   return 0;
}