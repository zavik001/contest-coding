#include <iostream>
#include <algorithm>
using namespace std;

void solution(int x, int y)
{
   int t = 10;

   auto summ = [x, t] {return x + a;};

   cout << summ(100);
}

int main ()
{
   solution(10, 10);
   return 0;
}